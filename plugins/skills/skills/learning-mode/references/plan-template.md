# Learning Plan

> This file is the user-editable source of truth for what they want to learn. Update it as goals shift.

## Engagement mode

`auto` — Claude proactively offers practice when topics come due or natural moments arise.
`manual` — Claude only coaches when explicitly invoked.

**Current mode:** `<auto | manual>`

## Stack

- Language: `<e.g., Python 3.11>`
- Frameworks: `<e.g., FastAPI, SQLAlchemy, pytest>`
- Other tooling: `<e.g., Docker, GitHub Actions>`

## Goals

What is the user trying to get to? One or two sentences each.

- `<goal 1>` — e.g., "Be comfortable shipping production-quality FastAPI services solo."
- `<goal 2>` — e.g., "Stop being afraid of async — write idiomatic asyncio without copy-pasting."

## Focus areas

Themes the user wants to deliberately practice. Each one will spawn multiple specific topics in `progress.md`.

- `<theme 1>` — e.g., "API design and request validation"
- `<theme 2>` — e.g., "Async concurrency patterns"
- `<theme 3>` — e.g., "Writing testable code"

## Level (per focus area)

A rough self-rating to calibrate task difficulty. Three buckets:

- **first contact** — new to this, needs scaffolding
- **comfortable** — knows basics, building fluency
- **polishing** — wants idiom, edge cases, advanced patterns

| Focus area | Level |
|------------|-------|
| `<area>`   | `<level>` |

## Out of scope

Topics the user explicitly does **not** want to work on right now. Helps Claude not drag them sideways.

- `<e.g., "no frontend stuff this quarter">`

## Notes

Anything the user wants Claude to remember about how they like to learn — preferred hint style, time available per session, etc.
